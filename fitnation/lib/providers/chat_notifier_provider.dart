// import 'package:flutter_riverpod/flutter_riverpod.dart';
// import 'package:fitnation/providers/chat_provider.dart';

// /// Shared ChatProvider instance used by both Riverpod and provider package
// /// consumers so they all interact with the same stateful object.
// final sharedChatProvider = ChatProvider();

// final chatNotifierProvider = ChangeNotifierProvider<ChatProvider>((ref) {
//   // Return the shared instance. Do NOT dispose here; the app owns the
//   // lifecycle (main.dart will not dispose it) so it remains available.
//   return sharedChatProvider;
// });
